// import { accessLogStream } from '../../../index.js';

// import { promises as fsPromises } from '../../../node_modules/fs';
// // Function to log messages to a file
// const logToFile = async (message) => {
//     try {
//       await fsPromises.appendFile('logfile.log', `${message}\n`);
//     } catch (err) {
//       console.error('Error writing to log file:', err);
//     }
//   };
  
//   // Usage example
//   console.log('This message will still appear in the console.');
